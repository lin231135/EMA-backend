import db from '../../db/connection.js';

// get metrics for admin dashboard
// we need to call methods of reports.controller.js to get the data
export const getDashboard = async (req, res) => {
  // TODO: Implement logic to update a trial request by ID
};